/*const myCity = {
    country: 'Ukrain',
    town: 'Kramatorsk',
region: 'Donetskaya'
}
console.log(myCity);

import { createServer } from 'http';
const PORT = 4100;
const server = createServer((req, res) => {
  res.end("aloha");
});

// localhost:4000
server.listen(4100);*/

